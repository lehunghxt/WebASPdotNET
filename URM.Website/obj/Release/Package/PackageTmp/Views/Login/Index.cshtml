@model URM.Model.URMUserLoginModel

@{
    ViewBag.Title = "Đăng nhập";    
    Layout = "~/Views/Shared/Blank.cshtml";
}

@using (Html.BeginForm())
{
    @*<script>
    $.ajax({
        url: 'http://localhost:51925/api/Authenticate/Post',
            type: "POST",
            dataType: "json",
            contentType: 'application/json',
            data: JSON.stringify({ UserName: "demo", Password: "1" }),
            success: function (data, status, xhr)
                {
                    console.log(res);
                }
    });
    </script>*@
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <div class="login-panel panel panel-default" style="margin-top:100px; margin-bottom:200px">
                <div class="panel-body">
                    <fieldset>
                        <p id="notvalid" runat="server" visible="false" class="text-error" style="color:red">
                            @ViewBag.Error
                        </p>
                        <div class="form-group">
                            @Html.EditorFor(model => model.UserName, new { htmlAttributes = new { @class = "form-control", placeholder = "Tên đăng nhập" } })
                        </div>
                        <div class="form-group">
                            @Html.EditorFor(model => model.Password, new { htmlAttributes = new { @class = "form-control", placeholder = "Mật khẩu", type = "password"} })
                        </div>
                        <div class="form-group">
                            @Html.EditorFor(model => model.ApplicationId, new { htmlAttributes = new { @class = "form-control", placeholder = "Mã ứng dụng", type = "password" } })
                        </div>
                        <div class="form-group">
                            <div class="row">
                                <div class="col-lg-6">
                                    @Html.CheckBoxFor(model => model.CreatePersistentCookie) Giữ chế độ đăng nhập
                                </div>
                                <div class="col-lg-6 text-right">
                                    <a href="/Application/Create">Đăng ký App mới</a>
                                </div>
                            </div>
                        </div>
                            
                            <!-- Change this to a button or input when using this as a form -->
                            <input type="submit" class="btn btn-lg btn-success btn-block" value="Đăng nhập"></input>
                        <div class="text-center" style="margin:15px"><a href="/Login/ForgetPassword">Quên mật khẩu</a></div>
</fieldset>
                </div>
            </div>
        </div>
    </div>
}